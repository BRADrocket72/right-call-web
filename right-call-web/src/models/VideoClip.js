export default class VideoClip {
    constructor(id, videoLength, videoUrl) {
        this.id = id;
        this.videoLength = videoLength;
        this.videoUrl = videoUrl
        // timestamp id
    }
}